openapi: 3.1.0
info:
  title: lxp-llm-gateway API
  version: 0.1.0
  description: Initial placeholder contract for gateway and admin APIs.
paths:
  /health:
    get:
      summary: Health check placeholder
      operationId: getHealth
      responses:
        "200":
          description: Service is healthy
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    example: ok
                required:
                  - status
components: {}
